/** @type {import('next').NextConfig} */
const nextConfig = {
    distDir: 'build',
    /*async redirects() {
        return [
            {
                source: '/',
                destination: '/categories/main',
                permanent: false
            },
        ]
    },*/
};
export default nextConfig;
